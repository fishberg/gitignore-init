[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gitignore-init"
version = "0.1.0"

[project.scripts]
gitignore-init = "gitignore_init.gitignore_init:main"
